# local kubeconfig / secrets — never commit
*.kubeconfig
admin.conf
kubeconfig
.env

# private inventories that may hold node passwords (bootstrap column) — never commit
*.local.conf
secrets.conf

# generated join commands (contain cluster tokens)
config/join-*.sh

# editor / OS cruft
.DS_Store
Thumbs.db
*.swp
*.bak
